public class SnakeBody {
    int x;
    int y;
    boolean isSnakeHead;

    public SnakeBody(int x, int y, boolean isSnakeHead) {
        this.x = x;
        this.y = y;
        this.isSnakeHead = isSnakeHead;
    }
}